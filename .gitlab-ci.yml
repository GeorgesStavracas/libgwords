image: fedora:26
stages:
  - build
before_script:
  - dnf install -y gcc meson ninja-build gettext gtk-doc glib2-devel
build-libgwords:
  stage: build
  script:
  - ./configure
  - make
  - make check
  - make install
